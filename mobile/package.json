{
  "name": "diamerna-mobile",
  "version": "1.0.0",
  "description": "DiaMerna — Maternal Wellness Mobile App",
  "main": "index.js",
  "scripts": {
    "build": "node ../encrypt-keys.js && npx cap sync && npx cap copy",
    "build:apk": "node ../encrypt-keys.js && npx cap sync && cd android && ./gradlew assembleRelease",
    "dev": "npx cap serve"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.0",
    "@capacitor/push-notifications": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
