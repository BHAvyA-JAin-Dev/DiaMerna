# Environment files (actual secrets — NOT committed)
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Node
node_modules/

# SQLite database
diamerna.db
*.db-shm
*.db-wal

# Vercel
.vercel

# User uploads (reports with personal data)
github/uploads/
dev/uploads/

# Local asset copy (already in dev/logo.jpeg)
assets/
